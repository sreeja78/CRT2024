#include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0)
    {
        printf("second no cannot be zero");
        b=1;
        }
    int c=(a/b);
    printf("%d",c);

    return 0;
}
